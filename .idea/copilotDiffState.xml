<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/Controller/AuthController.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Controller/AuthController.php" />
              <option name="originalContent" value="&lt;?php&#10;namespace Controller;&#10;&#10;use Psr\Container\ContainerInterface;&#10;use Slim\Psr7\Request;&#10;use Slim\Psr7\Response;&#10;use Model\UtenteRepository;&#10;use Firebase\JWT\JWT;&#10;use Firebase\JWT\Key;&#10;use Ramsey\Uuid\Uuid;&#10;&#10;class AuthController {&#10;    private $container;&#10;&#10;    public function __construct(ContainerInterface $container) {&#10;        $this-&gt;container = $container;&#10;    }&#10;&#10;    private function jsonResponse(Response $response, array $data, int $status = 200): Response {&#10;        $response-&gt;getBody()-&gt;write(json_encode($data));&#10;        return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus($status);&#10;    }&#10;&#10;    public function register(Request $request, Response $response): Response {&#10;        $data = $request-&gt;getParsedBody();&#10;        $repo = new UtenteRepository($this-&gt;container-&gt;get('config'));&#10;&#10;        if ($repo-&gt;getUtenteByEmail($data['email'])) {&#10;            return $this-&gt;jsonResponse($response, ['error' =&gt; 'Email già registrata'], 400);&#10;        }&#10;&#10;        $id = Uuid::uuid7()-&gt;toString();&#10;        $hash = password_hash($data['password'], PASSWORD_BCRYPT);&#10;&#10;        if ($repo-&gt;createUtente($id, $data['email'], $hash, $data['nome'], $data['cognome'])) {&#10;            return $this-&gt;jsonResponse($response, [&#10;                'id_utente' =&gt; $id, 'email' =&gt; $data['email'], 'nome' =&gt; $data['nome'], 'cognome' =&gt; $data['cognome'], 'ruolo' =&gt; 'utente'&#10;            ], 201);&#10;        }&#10;        return $this-&gt;jsonResponse($response, ['error' =&gt; 'Errore server'], 500);&#10;    }&#10;&#10;    public function login(Request $request, Response $response): Response {&#10;        $data = $request-&gt;getParsedBody();&#10;        $email = $data['username'] ?? $data['email'] ?? '';&#10;        $password = $data['password'] ?? '';&#10;&#10;        $repo = new UtenteRepository($this-&gt;container-&gt;get('config'));&#10;        $user = $repo-&gt;getUtenteByEmail($email);&#10;&#10;        if (!$user || !password_verify($password, $user['password_hash'])) {&#10;            return $this-&gt;jsonResponse($response, ['error' =&gt; 'Email o password non corretti'], 401);&#10;        }&#10;&#10;        $config = $this-&gt;container-&gt;get('config');&#10;        $payload = [&#10;            'iss' =&gt; &quot;parking_system&quot;, 'sub' =&gt; $user['email'], 'role' =&gt; $user['ruolo'],&#10;            'nome' =&gt; $user['nome'], 'cognome' =&gt; $user['cognome'], // Utili per cercare le prenotazioni poi&#10;            'iat' =&gt; time(), 'exp' =&gt; time() + (60 * 60 * 24) // 24 ore&#10;        ];&#10;&#10;        $jwt = JWT::encode($payload, $config['JWT_SECRET'], 'HS256');&#10;        return $this-&gt;jsonResponse($response, ['access_token' =&gt; $jwt, 'token_type' =&gt; 'bearer']);&#10;    }&#10;&#10;    public function me(Request $request, Response $response): Response {&#10;        // Leggiamo i dati dal Middleware!&#10;        $jwt = $request-&gt;getAttribute('jwt_payload');&#10;&#10;        $repo = new UtenteRepository($this-&gt;container-&gt;get('config'));&#10;        $user = $repo-&gt;getUtenteByEmail($jwt-&gt;sub);&#10;&#10;        if (!$user) return $this-&gt;jsonResponse($response, ['error' =&gt; 'Utente non trovato'], 404);&#10;        unset($user['password_hash']);&#10;        return $this-&gt;jsonResponse($response, $user);&#10;    }&#10;}" />
              <option name="updatedContent" value="&lt;?php&#10;namespace Controller;&#10;&#10;use Psr\Container\ContainerInterface;&#10;use Slim\Psr7\Request;&#10;use Slim\Psr7\Response;&#10;use Model\UtenteRepository;&#10;use Firebase\JWT\JWT;&#10;use Firebase\JWT\Key;&#10;use Ramsey\Uuid\Uuid;&#10;&#10;class AuthController {&#10;    private $container;&#10;&#10;    public function __construct(ContainerInterface $container) {&#10;        $this-&gt;container = $container;&#10;    }&#10;&#10;    private function jsonResponse(Response $response, array $data, int $status = 200): Response {&#10;        // Mette i dati in formato JSON e aggiunge l'intestazione corretta&#10;        $response-&gt;getBody()-&gt;write(json_encode($data));&#10;        return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus($status);&#10;    }&#10;&#10;    public function register(Request $request, Response $response): Response {&#10;        $data = $request-&gt;getParsedBody();&#10;        $repo = new UtenteRepository($this-&gt;container-&gt;get('config'));&#10;&#10;        // Controlla se l'email è già usata&#10;        if ($repo-&gt;getUtenteByEmail($data['email'])) {&#10;            return $this-&gt;jsonResponse($response, ['error' =&gt; 'Email già registrata'], 400);&#10;        }&#10;&#10;        $id = Uuid::uuid7()-&gt;toString();&#10;        $hash = password_hash($data['password'], PASSWORD_BCRYPT);&#10;&#10;        if ($repo-&gt;createUtente($id, $data['email'], $hash, $data['nome'], $data['cognome'])) {&#10;            return $this-&gt;jsonResponse($response, [&#10;                'id_utente' =&gt; $id, 'email' =&gt; $data['email'], 'nome' =&gt; $data['nome'], 'cognome' =&gt; $data['cognome'], 'ruolo' =&gt; 'utente'&#10;            ], 201);&#10;        }&#10;        return $this-&gt;jsonResponse($response, ['error' =&gt; 'Errore server'], 500);&#10;    }&#10;&#10;    public function login(Request $request, Response $response): Response {&#10;        $data = $request-&gt;getParsedBody();&#10;        $email = $data['username'] ?? $data['email'] ?? '';&#10;        $password = $data['password'] ?? '';&#10;&#10;        $repo = new UtenteRepository($this-&gt;container-&gt;get('config'));&#10;        $user = $repo-&gt;getUtenteByEmail($email);&#10;&#10;        // Controlla email e password&#10;        if (!$user || !password_verify($password, $user['password_hash'])) {&#10;            return $this-&gt;jsonResponse($response, ['error' =&gt; 'Email o password non corretti'], 401);&#10;        }&#10;&#10;        $config = $this-&gt;container-&gt;get('config');&#10;        $payload = [&#10;            'iss' =&gt; &quot;parking_system&quot;, 'sub' =&gt; $user['email'], 'role' =&gt; $user['ruolo'],&#10;            'nome' =&gt; $user['nome'], 'cognome' =&gt; $user['cognome'], // Utili per trovare le prenotazioni dell'utente&#10;            'iat' =&gt; time(), 'exp' =&gt; time() + (60 * 60 * 24) // Token valido per 24 ore&#10;        ];&#10;&#10;        $jwt = JWT::encode($payload, $config['JWT_SECRET'], 'HS256');&#10;        return $this-&gt;jsonResponse($response, ['access_token' =&gt; $jwt, 'token_type' =&gt; 'bearer']);&#10;    }&#10;&#10;    public function me(Request $request, Response $response): Response {&#10;        // Legge i dati dell'utente dal token che è stato verificato dal Middleware&#10;        $jwt = $request-&gt;getAttribute('jwt_payload');&#10;&#10;        $repo = new UtenteRepository($this-&gt;container-&gt;get('config'));&#10;        $user = $repo-&gt;getUtenteByEmail($jwt-&gt;sub);&#10;&#10;        if (!$user) return $this-&gt;jsonResponse($response, ['error' =&gt; 'Utente non trovato'], 404);&#10;        unset($user['password_hash']); // Non mandare la password al client&#10;        return $this-&gt;jsonResponse($response, $user);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/Controller/ParkingLotController.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Controller/ParkingLotController.php" />
              <option name="originalContent" value="&lt;?php&#10;namespace Controller;&#10;&#10;use Psr\Container\ContainerInterface;&#10;use Slim\Psr7\Request;&#10;use Slim\Psr7\Response;&#10;use Model\ParkingLotRepository;&#10;&#10;class ParkingLotController {&#10;    private $container;&#10;&#10;    public function __construct(ContainerInterface $container) {&#10;        $this-&gt;container = $container;&#10;    }&#10;&#10;    public function listAll(Request $request, Response $response): Response {&#10;        $repo = new ParkingLotRepository($this-&gt;container-&gt;get('config'));&#10;        $response-&gt;getBody()-&gt;write(json_encode($repo-&gt;getAllParkingLots()));&#10;        return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(200);&#10;    }&#10;&#10;    public function create(Request $request, Response $response): Response {&#10;        $data = $request-&gt;getParsedBody();&#10;        $repo = new ParkingLotRepository($this-&gt;container-&gt;get('config'));&#10;&#10;        if ($repo-&gt;createParkingLot($data['name'], $data['total_spots'])) {&#10;            $response-&gt;getBody()-&gt;write(json_encode(['message' =&gt; 'Parcheggio creato']));&#10;            return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(201);&#10;        }&#10;        $response-&gt;getBody()-&gt;write(json_encode(['error' =&gt; 'Errore']));&#10;        return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(500);&#10;    }&#10;&#10;    public function delete(Request $request, Response $response, array $args): Response {&#10;        $repo = new ParkingLotRepository($this-&gt;container-&gt;get('config'));&#10;        if ($repo-&gt;deleteParkingLot((int)$args['id'])) {&#10;            $response-&gt;getBody()-&gt;write(json_encode(['message' =&gt; 'Parcheggio eliminato']));&#10;            return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(200);&#10;        }&#10;        $response-&gt;getBody()-&gt;write(json_encode(['error' =&gt; 'Errore']));&#10;        return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(500);&#10;    }&#10;}" />
              <option name="updatedContent" value="&lt;?php&#10;namespace Controller;&#10;&#10;use Psr\Container\ContainerInterface;&#10;use Slim\Psr7\Request;&#10;use Slim\Psr7\Response;&#10;use Model\ParkingLotRepository;&#10;&#10;class ParkingLotController {&#10;    private $container;&#10;&#10;    public function __construct(ContainerInterface $container) {&#10;        $this-&gt;container = $container;&#10;    }&#10;&#10;    public function listAll(Request $request, Response $response): Response {&#10;        // Recupera e restituisce tutti i parcheggi dal database&#10;        $repo = new ParkingLotRepository($this-&gt;container-&gt;get('config'));&#10;        $response-&gt;getBody()-&gt;write(json_encode($repo-&gt;getAllParkingLots()));&#10;        return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(200);&#10;    }&#10;&#10;    public function create(Request $request, Response $response): Response {&#10;        // Crea un nuovo parcheggio&#10;        $data = $request-&gt;getParsedBody();&#10;        $repo = new ParkingLotRepository($this-&gt;container-&gt;get('config'));&#10;&#10;        if ($repo-&gt;createParkingLot($data['name'], $data['total_spots'])) {&#10;            $response-&gt;getBody()-&gt;write(json_encode(['message' =&gt; 'Parcheggio creato']));&#10;            return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(201);&#10;        }&#10;        $response-&gt;getBody()-&gt;write(json_encode(['error' =&gt; 'Errore']));&#10;        return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(500);&#10;    }&#10;&#10;    public function delete(Request $request, Response $response, array $args): Response {&#10;        // Elimina un parcheggio dal database&#10;        $repo = new ParkingLotRepository($this-&gt;container-&gt;get('config'));&#10;        if ($repo-&gt;deleteParkingLot((int)$args['id'])) {&#10;            $response-&gt;getBody()-&gt;write(json_encode(['message' =&gt; 'Parcheggio eliminato']));&#10;            return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(200);&#10;        }&#10;        $response-&gt;getBody()-&gt;write(json_encode(['error' =&gt; 'Errore']));&#10;        return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(500);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/Middleware/JwtMiddleware.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Middleware/JwtMiddleware.php" />
              <option name="originalContent" value="&lt;?php&#10;namespace Middleware;&#10;&#10;use Psr\Http\Message\ResponseInterface as Response;&#10;use Psr\Http\Message\ServerRequestInterface as Request;&#10;use Psr\Http\Server\RequestHandlerInterface as RequestHandler;&#10;use Slim\Psr7\Response as SlimResponse;&#10;use Firebase\JWT\JWT;&#10;use Firebase\JWT\Key;&#10;&#10;class JwtMiddleware {&#10;    private $secret;&#10;&#10;    public function __construct(string $secret) {&#10;        $this-&gt;secret = $secret;&#10;    }&#10;&#10;    public function __invoke(Request $request, RequestHandler $handler): Response {&#10;        $header = $request-&gt;getHeaderLine('Authorization');&#10;&#10;        // Controlla se il token c'è&#10;        if (!$header || !preg_match('/Bearer\s(\S+)/', $header, $matches)) {&#10;            $response = new SlimResponse();&#10;            $response-&gt;getBody()-&gt;write(json_encode(['error' =&gt; 'Autenticazione richiesta']));&#10;            return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(401);&#10;        }&#10;&#10;        try {&#10;            $token = $matches[1];&#10;            // Tenta di decodificare il token&#10;            $decoded = JWT::decode($token, new Key($this-&gt;secret, 'HS256'));&#10;&#10;            // TRUCCO BUGATTI: Salviamo il payload del token dentro la Request!&#10;            // Così i Controller potranno leggerlo senza doverlo decodificare di nuovo.&#10;            $request = $request-&gt;withAttribute('jwt_payload', $decoded);&#10;&#10;        } catch (\Exception $e) {&#10;            $response = new SlimResponse();&#10;            $response-&gt;getBody()-&gt;write(json_encode(['error' =&gt; 'Token non valido o scaduto']));&#10;            return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(401);&#10;        }&#10;&#10;        // Tutto ok, lascia passare la richiesta al Controller&#10;        return $handler-&gt;handle($request);&#10;    }&#10;}" />
              <option name="updatedContent" value="&lt;?php&#10;namespace Middleware;&#10;&#10;use Psr\Http\Message\ResponseInterface as Response;&#10;use Psr\Http\Message\ServerRequestInterface as Request;&#10;use Psr\Http\Server\RequestHandlerInterface as RequestHandler;&#10;use Slim\Psr7\Response as SlimResponse;&#10;use Firebase\JWT\JWT;&#10;use Firebase\JWT\Key;&#10;&#10;class JwtMiddleware {&#10;    private $secret;&#10;&#10;    public function __construct(string $secret) {&#10;        $this-&gt;secret = $secret;&#10;    }&#10;&#10;    public function __invoke(Request $request, RequestHandler $handler): Response {&#10;        $header = $request-&gt;getHeaderLine('Authorization');&#10;&#10;        // Controlla se il token è presente nel header Authorization&#10;        if (!$header || !preg_match('/Bearer\s(\S+)/', $header, $matches)) {&#10;            $response = new SlimResponse();&#10;            $response-&gt;getBody()-&gt;write(json_encode(['error' =&gt; 'Autenticazione richiesta']));&#10;            return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(401);&#10;        }&#10;&#10;        try {&#10;            $token = $matches[1];&#10;            // Decodifica e verifica il token JWT&#10;            $decoded = JWT::decode($token, new Key($this-&gt;secret, 'HS256'));&#10;&#10;            // Salva il contenuto del token nella richiesta&#10;            // Così i Controller possono leggerlo senza doverlo decodificare di nuovo&#10;            $request = $request-&gt;withAttribute('jwt_payload', $decoded);&#10;&#10;        } catch (\Exception $e) {&#10;            $response = new SlimResponse();&#10;            $response-&gt;getBody()-&gt;write(json_encode(['error' =&gt; 'Token non valido o scaduto']));&#10;            return $response-&gt;withHeader('Content-Type', 'application/json')-&gt;withStatus(401);&#10;        }&#10;&#10;        // Token ok, lascia passare la richiesta al Controller&#10;        return $handler-&gt;handle($request);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/Model/ParkingLotRepository.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Model/ParkingLotRepository.php" />
              <option name="originalContent" value="&lt;?php&#10;namespace Model;&#10;use Util\Connection;&#10;use PDO;&#10;&#10;class ParkingLotRepository {&#10;    private $config;&#10;&#10;    public function __construct($config){&#10;        $this-&gt;config = $config;&#10;    }&#10;&#10;    public function getAllParkingLots(): array {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;query('SELECT * FROM parking_lot');&#10;        return $stmt-&gt;fetchAll(PDO::FETCH_ASSOC);&#10;    }&#10;&#10;    public function createParkingLot(string $name, int $total_spots): bool {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('INSERT INTO parking_lot (name, total_spots) VALUES (:name, :total_spots)');&#10;        return $stmt-&gt;execute(['name' =&gt; $name, 'total_spots' =&gt; $total_spots]);&#10;    }&#10;&#10;    public function deleteParkingLot(int $id): bool {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('DELETE FROM parking_lot WHERE id = :id');&#10;        return $stmt-&gt;execute(['id' =&gt; $id]);&#10;    }&#10;}" />
              <option name="updatedContent" value="&lt;?php&#10;namespace Model;&#10;use Util\Connection;&#10;use PDO;&#10;&#10;class ParkingLotRepository {&#10;    private $config;&#10;&#10;    public function __construct($config){&#10;        $this-&gt;config = $config;&#10;    }&#10;&#10;    public function getAllParkingLots(): array {&#10;        // Prende tutti i parcheggi dal database&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;query('SELECT * FROM parking_lot');&#10;        return $stmt-&gt;fetchAll(PDO::FETCH_ASSOC);&#10;    }&#10;&#10;    public function createParkingLot(string $name, int $total_spots): bool {&#10;        // Crea un nuovo parcheggio nel database&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('INSERT INTO parking_lot (name, total_spots) VALUES (:name, :total_spots)');&#10;        return $stmt-&gt;execute(['name' =&gt; $name, 'total_spots' =&gt; $total_spots]);&#10;    }&#10;&#10;    public function deleteParkingLot(int $id): bool {&#10;        // Elimina un parcheggio dal database&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('DELETE FROM parking_lot WHERE id = :id');&#10;        return $stmt-&gt;execute(['id' =&gt; $id]);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/Model/ReservationRepository.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Model/ReservationRepository.php" />
              <option name="originalContent" value="&lt;?php&#10;namespace Model;&#10;use Util\Connection;&#10;use PDO;&#10;&#10;class ReservationRepository {&#10;    private $config;&#10;&#10;    public function __construct($config){&#10;        $this-&gt;config = $config;&#10;    }&#10;&#10;    public function createReservation(string $id, int $parking_lot_id, string $first_name, string $last_name, string $license_plate, string $start_time, string $end_time): bool {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('&#10;            INSERT INTO reservation (id, parking_lot_id, first_name, last_name, license_plate, start_time, end_time, status) &#10;            VALUES (:id, :parking_lot_id, :first_name, :last_name, :license_plate, :start_time, :end_time, &quot;ACTIVE&quot;)&#10;        ');&#10;        return $stmt-&gt;execute([&#10;            'id' =&gt; $id, 'parking_lot_id' =&gt; $parking_lot_id, 'first_name' =&gt; $first_name,&#10;            'last_name' =&gt; $last_name, 'license_plate' =&gt; $license_plate,&#10;            'start_time' =&gt; $start_time, 'end_time' =&gt; $end_time&#10;        ]);&#10;    }&#10;&#10;    // Nuova funzione per prendere le prenotazioni di un utente (cercando per nome e cognome o targa)&#10;    // Nota: in un sistema reale si legherebbe l'id_utente alla prenotazione, ma usiamo i dati a disposizione&#10;    public function getReservationsByUser(string $first_name, string $last_name): array {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('&#10;            SELECT r.*, p.name as parking_lot_name &#10;            FROM reservation r &#10;            JOIN parking_lot p ON r.parking_lot_id = p.id &#10;            WHERE r.first_name = :first_name AND r.last_name = :last_name&#10;            ORDER BY r.created_at DESC&#10;        ');&#10;        $stmt-&gt;execute(['first_name' =&gt; $first_name, 'last_name' =&gt; $last_name]);&#10;        return $stmt-&gt;fetchAll(PDO::FETCH_ASSOC);&#10;    }&#10;}" />
              <option name="updatedContent" value="&lt;?php&#10;namespace Model;&#10;use Util\Connection;&#10;use PDO;&#10;&#10;class ReservationRepository {&#10;    private $config;&#10;&#10;    public function __construct($config){&#10;        $this-&gt;config = $config;&#10;    }&#10;&#10;    public function createReservation(string $id, int $parking_lot_id, string $first_name, string $last_name, string $license_plate, string $start_time, string $end_time): bool {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('&#10;            INSERT INTO reservation (id, parking_lot_id, first_name, last_name, license_plate, start_time, end_time, status) &#10;            VALUES (:id, :parking_lot_id, :first_name, :last_name, :license_plate, :start_time, :end_time, &quot;ACTIVE&quot;)&#10;        ');&#10;        return $stmt-&gt;execute([&#10;            'id' =&gt; $id, 'parking_lot_id' =&gt; $parking_lot_id, 'first_name' =&gt; $first_name,&#10;            'last_name' =&gt; $last_name, 'license_plate' =&gt; $license_plate,&#10;            'start_time' =&gt; $start_time, 'end_time' =&gt; $end_time&#10;        ]);&#10;    }&#10;&#10;    // Prende tutte le prenotazioni di un utente cercando per nome e cognome&#10;    // Nota: idealmente il sistema dovrebbe salvare l'ID utente nella prenotazione,&#10;    // ma qui usiamo i dati che abbiamo nel token&#10;    public function getReservationsByUser(string $first_name, string $last_name): array {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('&#10;            SELECT r.*, p.name as parking_lot_name &#10;            FROM reservation r &#10;            JOIN parking_lot p ON r.parking_lot_id = p.id &#10;            WHERE r.first_name = :first_name AND r.last_name = :last_name&#10;            ORDER BY r.created_at DESC&#10;        ');&#10;        $stmt-&gt;execute(['first_name' =&gt; $first_name, 'last_name' =&gt; $last_name]);&#10;        return $stmt-&gt;fetchAll(PDO::FETCH_ASSOC);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/Model/UtenteRepository.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Model/UtenteRepository.php" />
              <option name="originalContent" value="&lt;?php&#10;namespace Model;&#10;use Util\Connection;&#10;use PDO;&#10;&#10;class UtenteRepository {&#10;    private $config;&#10;&#10;    public function __construct($config){&#10;        $this-&gt;config = $config;&#10;    }&#10;&#10;    public function getUtenteByEmail(string $email) {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('SELECT * FROM utenti WHERE email = :email');&#10;        $stmt-&gt;execute(['email' =&gt; $email]);&#10;        return $stmt-&gt;fetch(PDO::FETCH_ASSOC);&#10;    }&#10;&#10;    public function createUtente(string $id, string $email, string $hash, string $nome, string $cognome): bool {&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('&#10;            INSERT INTO utenti (id_utente, email, password_hash, nome, cognome, ruolo) &#10;            VALUES (:id, :email, :hash, :nome, :cognome, &quot;utente&quot;)&#10;        ');&#10;        return $stmt-&gt;execute([&#10;            'id' =&gt; $id, 'email' =&gt; $email, 'hash' =&gt; $hash, 'nome' =&gt; $nome, 'cognome' =&gt; $cognome&#10;        ]);&#10;    }&#10;}" />
              <option name="updatedContent" value="&lt;?php&#10;namespace Model;&#10;use Util\Connection;&#10;use PDO;&#10;&#10;class UtenteRepository {&#10;    private $config;&#10;&#10;    public function __construct($config){&#10;        $this-&gt;config = $config;&#10;    }&#10;&#10;    public function getUtenteByEmail(string $email) {&#10;        // Cerca un utente tramite la sua email&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('SELECT * FROM utenti WHERE email = :email');&#10;        $stmt-&gt;execute(['email' =&gt; $email]);&#10;        return $stmt-&gt;fetch(PDO::FETCH_ASSOC);&#10;    }&#10;&#10;    public function createUtente(string $id, string $email, string $hash, string $nome, string $cognome): bool {&#10;        // Crea un nuovo utente nel database con i dati ricevuti&#10;        $pdo = Connection::getInstance($this-&gt;config);&#10;        $stmt = $pdo-&gt;prepare('&#10;            INSERT INTO utenti (id_utente, email, password_hash, nome, cognome, ruolo) &#10;            VALUES (:id, :email, :hash, :nome, :cognome, &quot;utente&quot;)&#10;        ');&#10;        return $stmt-&gt;execute([&#10;            'id' =&gt; $id, 'email' =&gt; $email, 'hash' =&gt; $hash, 'nome' =&gt; $nome, 'cognome' =&gt; $cognome&#10;        ]);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/Util/Connection.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/Util/Connection.php" />
              <option name="originalContent" value="&lt;?php&#10;&#10;//Il namespace deve essere uguale alla cartella che contiene il file&#10;namespace Util;&#10;use PDO;&#10;&#10;&#10;/**&#10; * Classe per gestire la connessione al database&#10; */&#10;&#10;class Connection&#10;{&#10;    //Statico perchè è un attributo di classe istanziato una sola volta&#10;    private static PDO $pdo;&#10;&#10;    /**&#10;     * Costruttore privato per evitare la creazione di oggetti&#10;     */&#10;    private function __construct()&#10;    {&#10;&#10;    }&#10;&#10;    public static function getInstance($config): PDO&#10;    {&#10;        if (!isset($pdo)) {&#10;            $DSN = 'mysql:host=' . $config['DB_HOST'] . ';dbname=' . $config['DB_NAME'];&#10;            $pdo = new PDO($DSN, $config['DB_USER'], $config['DB_PASS']);&#10;            //Impostiamo la &quot;forma&quot; dei dati che verranno restituiti da una&#10;            //query come delle mappe associative&#10;            $pdo-&gt;setAttribute(PDO::ATTR_DEFAULT_FETCH_MODE, PDO::FETCH_ASSOC);&#10;&#10;        }&#10;        return $pdo;&#10;    }&#10;}&#10;&#10;" />
              <option name="updatedContent" value="&lt;?php&#10;&#10;// Il namespace deve essere uguale al nome della cartella&#10;namespace Util;&#10;use PDO;&#10;&#10;// Classe che gestisce la connessione al database&#10;// Usa il pattern Singleton (una sola connessione per tutta l'app)&#10;class Connection&#10;{&#10;    // Attributo statico perché appartiene alla classe, non all'oggetto&#10;    private static PDO $pdo;&#10;&#10;    // Costruttore privato per evitare di creare oggetti di questa classe&#10;    private function __construct()&#10;    {&#10;&#10;    }&#10;&#10;    public static function getInstance($config): PDO&#10;    {&#10;        // Se la connessione non esiste ancora, la crea&#10;        if (!isset($pdo)) {&#10;            $DSN = 'mysql:host=' . $config['DB_HOST'] . ';dbname=' . $config['DB_NAME'];&#10;            $pdo = new PDO($DSN, $config['DB_USER'], $config['DB_PASS']);&#10;            // Imposta il modo di restituzione dei dati come array associativo&#10;            $pdo-&gt;setAttribute(PDO::ATTR_DEFAULT_FETCH_MODE, PDO::FETCH_ASSOC);&#10;        }&#10;&#10;        return $pdo;&#10;    }&#10;}&#10;&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/conf/config.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/conf/config.php" />
              <option name="originalContent" value="&lt;?php&#10;// Configura questi parametri in base al tuo ambiente (XAMPP o Docker)&#10;const DB_HOST = 'database';&#10;const DB_NAME = 'Parcheggio';&#10;const DB_USER = 'root';&#10;const DB_PASS = 'rootpassword';&#10;const BASEPATH = '/~paglia/parcheggio';&#10;const JWT_SECRET = 'tuo-super-segreto-parking-2026-cambia-in-prod';&#10;&#10;return [&#10;    'DB_HOST' =&gt; DB_HOST,&#10;    'DB_NAME' =&gt; DB_NAME,&#10;    'DB_USER' =&gt; DB_USER,&#10;    'DB_PASS' =&gt; DB_PASS,&#10;    'BASEPATH' =&gt; BASEPATH,&#10;    'JWT_SECRET' =&gt; JWT_SECRET&#10;];" />
              <option name="updatedContent" value="&lt;?php&#10;// Modifica questi valori in base a dove stai facendo girare il progetto&#10;const DB_HOST = 'database';&#10;const DB_NAME = 'Parcheggio';&#10;const DB_USER = 'root';&#10;const DB_PASS = 'rootpassword';&#10;const BASEPATH = '/~paglia/parcheggio';&#10;const JWT_SECRET = 'tuo-super-segreto-parking-2026-cambia-in-prod';&#10;&#10;return [&#10;    'DB_HOST' =&gt; DB_HOST,&#10;    'DB_NAME' =&gt; DB_NAME,&#10;    'DB_USER' =&gt; DB_USER,&#10;    'DB_PASS' =&gt; DB_PASS,&#10;    'BASEPATH' =&gt; BASEPATH,&#10;    'JWT_SECRET' =&gt; JWT_SECRET&#10;];" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/index.php">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/index.php" />
              <option name="originalContent" value="&lt;?php&#10;&#10;use Psr\Http\Message\ResponseInterface as Response;&#10;use Psr\Http\Message\ServerRequestInterface as Request;&#10;use Slim\Factory\AppFactory;&#10;use DI\Container as Container;&#10;require 'Middleware/JwtMiddleware.php';&#10;use Middleware\JwtMiddleware;&#10;&#10;// Autoload&#10;require '../vendor/autoload.php';&#10;&#10;// Model &amp; Util&#10;require 'Util/Connection.php';&#10;require 'Model/UtenteRepository.php';&#10;require 'Model/ParkingLotRepository.php';&#10;require 'Model/ReservationRepository.php';&#10;&#10;// Controller&#10;require 'Controller/AuthController.php';&#10;require 'Controller/ParkingLotController.php';&#10;require 'Controller/ReservationController.php';&#10;&#10;use Controller\AuthController;&#10;use Controller\ParkingLotController;&#10;use Controller\ReservationController;&#10;&#10;// Configurazione&#10;$config = require 'conf/config.php';&#10;$container = new Container();&#10;AppFactory::setContainer($container);&#10;$app = AppFactory::create();&#10;&#10;$app-&gt;setBasePath($config['BASEPATH']);&#10;$app-&gt;addBodyParsingMiddleware();&#10;$container-&gt;set('config', $config);&#10;&#10;&#10;// ============================================================================&#10;// --- ROTTE PUBBLICHE (Non serve il Token) ---&#10;// ============================================================================&#10;$app-&gt;post('/api/v1/auth/register', AuthController::class . ':register');&#10;$app-&gt;post('/api/v1/auth/token', AuthController::class . ':login');&#10;// I parcheggi devono essere pubblici per poterli disegnare sulla mappa di React all'avvio&#10;$app-&gt;get('/api/v1/parking-lots', ParkingLotController::class . ':listAll');&#10;&#10;// ============================================================================&#10;// --- ROTTE PROTETTE (Il Middleware controlla il Token) ---&#10;// ============================================================================&#10;$app-&gt;group('/api/v1', function ($group) {&#10;    // Profilo utente&#10;    $group-&gt;get('/auth/me', AuthController::class . ':me');&#10;&#10;    // Gestione Parcheggi (Admin)&#10;    $group-&gt;post('/parking-lots', ParkingLotController::class . ':create');&#10;    $group-&gt;delete('/parking-lots/{id}', ParkingLotController::class . ':delete');&#10;&#10;    // Prenotazioni&#10;    $group-&gt;post('/reservations', ReservationController::class . ':create');&#10;    $group-&gt;get('/reservations/user', ReservationController::class . ':listByUser');&#10;&#10;})-&gt;add(new JwtMiddleware($config['JWT_SECRET'])); // &lt;--- APPLICHIAMO IL MIDDLEWARE!&#10;&#10;$app-&gt;run();" />
              <option name="updatedContent" value="&lt;?php&#10;&#10;use Psr\Http\Message\ResponseInterface as Response;&#10;use Psr\Http\Message\ServerRequestInterface as Request;&#10;use Slim\Factory\AppFactory;&#10;use DI\Container as Container;&#10;require 'Middleware/JwtMiddleware.php';&#10;use Middleware\JwtMiddleware;&#10;&#10;// Autoload&#10;require '../vendor/autoload.php';&#10;&#10;// Model &amp; Util&#10;require 'Util/Connection.php';&#10;require 'Model/UtenteRepository.php';&#10;require 'Model/ParkingLotRepository.php';&#10;require 'Model/ReservationRepository.php';&#10;&#10;// Controller&#10;require 'Controller/AuthController.php';&#10;require 'Controller/ParkingLotController.php';&#10;require 'Controller/ReservationController.php';&#10;&#10;use Controller\AuthController;&#10;use Controller\ParkingLotController;&#10;use Controller\ReservationController;&#10;&#10;// Configurazione&#10;$config = require 'conf/config.php';&#10;$container = new Container();&#10;AppFactory::setContainer($container);&#10;$app = AppFactory::create();&#10;&#10;$app-&gt;setBasePath($config['BASEPATH']);&#10;$app-&gt;addBodyParsingMiddleware();&#10;$container-&gt;set('config', $config);&#10;&#10;&#10;// ROTTE CHE CHIUNQUE PUÒ USARE (senza login)&#10;$app-&gt;post('/api/v1/auth/register', AuthController::class . ':register');&#10;$app-&gt;post('/api/v1/auth/token', AuthController::class . ':login');&#10;// I parcheggi si vedono per tutti per mostrarli sulla mappa&#10;$app-&gt;get('/api/v1/parking-lots', ParkingLotController::class . ':listAll');&#10;&#10;// ROTTE PROTETTE (serve il token per accedere)&#10;$app-&gt;group('/api/v1', function ($group) {&#10;    // Informazioni dell'utente loggato&#10;    $group-&gt;get('/auth/me', AuthController::class . ':me');&#10;&#10;    // Creare e eliminare parcheggi&#10;    $group-&gt;post('/parking-lots', ParkingLotController::class . ':create');&#10;    $group-&gt;delete('/parking-lots/{id}', ParkingLotController::class . ':delete');&#10;&#10;    // Creare prenotazioni e vedere le tue prenotazioni&#10;    $group-&gt;post('/reservations', ReservationController::class . ':create');&#10;    $group-&gt;get('/reservations/user', ReservationController::class . ':listByUser');&#10;&#10;// Controlla il token JWT per tutte le rotte di questo gruppo&#10;})-&gt;add(new JwtMiddleware($config['JWT_SECRET']));&#10;&#10;$app-&gt;run();" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>